# Этап 2. Текстуры граней ✅ выполнен

1. Бейкер `src/client/bakers/tankModelTexture.js` — атлас на команду: верх корпуса и палубы, верх башни — из
   нынешнего рисунка (`tankTexture.js`, общие функции рисования, цвет команды на башне); боковые грани гусениц
   (катки, траки), борта корпуса, скаты башни, ствол и тормоз — свои полосы. Светотень атласа — только симметричная
   (направленный свет даёт `faceShade`).
2. UV граней: у каждого материала модели — прямоугольник атласа; верх корпуса и башни проецируется по `(u, v)`
   (совпадение с рисунком), бока — по длине ребра × высоте.
3. Регистрация бейкера в `src/client/bakers/index.js` и `src/config/client.js` (ассет `tankModelTexture` для `Tank`).
4. Тесты: размеры атласа, прямоугольники материалов не пересекаются, UV всех граней внутри своего прямоугольника.
5. Проверки: `npx eslint .`, `npm test -- --silent`, `npm run build`.

## Ход выполнения

- `tankTexture.js`: рисование корпуса и башни вынесено в экспортируемые `drawTankBody`, `drawTankGun` (+ `TANK_ART` —
  габарит рисунка); плоская текстура не изменилась (её тесты зелёные).
- `src/client/bakers/tankModelTexture.js` — атлас на команду (`liveTeamId1/2`: `texture`, `width`, `height`,
  `regions`): `body` и `gun` — рисунок сверху с `originX/originY` (точка (0, 0) модели), полосы `trackSide` (катки),
  `barrelSide`, `brakeSide`, `bottom`; зазор 2 px. Зарегистрирован в `bakers/index.js` и `client.js` (для `Tank`).
- `src/client/tank3d/uv.js` — `faceUVs`: верхи и скаты планарно по (u, v) непозированной модели (гусеницы и корпус —
  `body`, башня и ствол — `gun`), отвесные бока — на полосы по длине ребра, низ — точка `bottom`.
- Попутно исправлено в модели: ствол `u ∈ [2.5, 21]` (упирается в тормоз; раньше 20.8 — ширина рисунка, принятая
  за конец).
- Тесты: `tests/client/bakers/tankModelTexture.test.js`, `tests/client/tank3d/uv.test.js`.
- `npx eslint .`, `npm test` (724), `npm run build`, `npx vimp-contract` — зелёные. CHANGELOG и документация — на
  этапе 3, когда модель появится в игре.
