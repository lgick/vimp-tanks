# План: дрейф предсказания на контактах реплики

## Контекст

Итерация «Кодревью многоуровневых карт 2.5D» закрыта
(`plan/done/review-multilevel-2/`), танки подготовлены к релизу `0.19.0`,
но `npm run sim:scenarios` даёт **8 зелёных сценариев из 12**. Четыре валят
контракт `predictionDrift`, и это два РАЗНЫХ дефекта:

| Сценарий | Нарушений | Поля | Дефект |
| --- | --- | --- | --- |
| `bridge.json`, `crosslevel.json`, `fall.json` | 1 из ~350 (в `crosslevel` — 2) | `angle` Δ0.2063 (порог 0.06), `angvel` Δ2.8059 (порог 1.5) | касательный удар об угол перил |
| `terraces_climb.json` | 24 из 198 | `x` Δ2.2314 (порог 3 — не нарушает), `vx` Δ27.92 (порог 25), уровень Δ0.15 | своё, более раннее расхождение |

Дефект настоящий, не шум теста: в игре это кадр рывка при задевании перил
углом корпуса. Числа совпадают с базовой линией до итерации — регрессии нет,
долг накоплен раньше.

Сейчас долг описан в `docs/en/getting-started.md` и
`docs/ru/getting-started.md` (блоки «Known debt» / «Известный долг» перед
разделом про калибровку порогов). Задача: **вынести его из документации в
отдельный план проекта и починить**.

### Что уже проверено — не повторять

Прошлая итерация закрыла ложные версии причины, замеры сохранены в
`plan/done/review-multilevel-2/stage_7.md` (§ «Что выяснилось при исполнении
танковой части»):

- **Не геометрия карты.** Хост и реплика читают стены ОДНИМИ склеенными
  блоками (`MapLevels::static_blocks` → `collect_block_contacts`). Прямой
  замер `parry::query::contact` на разобранной позе: и склеенный блок
  (центр `(345.6, 403.2)`, полуразмеры `(268.8, 6.4)`), и одиночный угловой
  тайл дают одну нормаль `(1, 0)` и одну глубину `1.10599` — у них общие
  рёбра `x = 76.8` и `y = 396.8`.
- Не уровни, не маски, не стражи прогона, не вердикт гейта, не откат
  `LevelState`, не константы подъёма — каждое откатывалось по отдельности,
  расхождение оставалось.

### Причина (установлена, но не устранена)

Разное **разрешение контакта**, а не разная стена.

Хост (`core/src/tank.rs:160-185`): Rapier, корпус — `cuboid(4, 3)`
(`model.size = 2` → 8 × 6), `soft_ccd_prediction(width.min(height)) = 6`.
Rapier строит СПЕКУЛЯТИВНЫЙ контакт, пока тела ещё разведены, ведёт
многоточечный манифольд и решает скорости ДО интеграции позиции.

Реплика (`core/src/client/predictor.rs`):

1. `step_inner` (:707) интегрирует позицию скоростью (:768-776) и только
   потом зовёт `resolve_world(dt)` (:777);
2. `resolve_world` (:831) собирает контакты по УЖЕ перекрытому OBB —
   один контакт на пару, вектор минимального выталкивания
   (`obb_vs_obb` / `collect_block_contacts` в
   `vimp-engine-core::client::collision`);
3. дальше `separate_bodies` один раз на контакт и `SOLVER_ITERATIONS = 4`
   (:48) проходов `apply_contact_impulse`
   (`vimp-engine-core::client::rigid_body:118` и `:136`).

Следствия: на скорости 148.85 ед/с танк за шаг 1/120 уходит в стену на
1.24 ед. и реагирует уже изнутри; единственная точка контакта даёт другое
плечо, чем манифольд Rapier, — отсюда `angvel`; при глубоком касательном
перекрытии ось MTV вообще может оказаться другой.

Замер того самого шага (`bridge.json`, serverTime 1700000003500):

| | до шага | после шага |
| --- | --- | --- |
| хост | `x=72.61 y=395.07 v=(148.85, 0.87)`, уровень 1, `Grounded` | `x=72.93 y=394.80 v=(7.29, -40.45) w=5.521` |
| реплика | то же самое, бит в бит | `x=72.81 y=395.08 v=(-7.65, -0.04) w=0.000` |

Авторитетная строка кадра: `[73.27, 394.18, 0.212, 12.39, -18.25, 2.806, 0, 1]`.

### Итог задачи

12 из 12 сценариев зелёные при НЕТРОНУТЫХ порогах
(`divergence.thresholds` в `tests/scenarios/*.json`, у `bridge.json` это
`[3, 3, 0.06, 25, 25, 1.5, 0.15, 0.06]`). Пороги ослаблять запрещено: они и
есть определение «предсказание совпало с сервером».

## Репозитории и правила

| Обозначение | Путь | Публикуется |
| --- | --- | --- |
| **E** | `/Users/dmitry/Sites/my/vimp` | крейт `vimp-engine-core` (crates.io) + npm `vimp-engine` |
| **T** | `/Users/dmitry/Sites/my/vimp-tanks` | npm `@vimp-games/tanks` |

Текущие версии: `vimp-engine-core 0.14.1`, `vimp-engine 0.32.1`,
`@vimp-games/tanks 0.19.0` (в рабочем дереве, ещё не опубликован).

Правила работы (из `CLAUDE.md` обоих репозиториев и глобальных инструкций):

- **Коммитов не делать.** Всё остаётся в рабочем дереве на ревью.
- Примитивы контактов живут в движке (`packages/engine/core/src/client/`),
  поэтому правка идёт «E → релиз крейта → T». Танки зависят от крейта по
  версии, а не по пути; на время отладки допустим временный
  `[patch.crates-io]` в `core/Cargo.toml` танков — **снять перед релизом**.
- Любая функциональная правка обновляет `docs/en/` И `docs/ru/`
  (симметрично) и `CHANGELOG.md` под `## [Unreleased]` в том же изменении.
  Тесты, рефакторинги и правки `docs/` в журнал не идут.
- Планы — в `plan/`, по-русски; выполненный этап помечается «✅ выполнен»;
  завершённый план целиком переезжает в `plan/done/` через `git mv`.

## Этап 0. Вынести долг из документации в план ✅ выполнен

1. ✅ Создан `plan/prediction-drift.md` (этот файл).
2. ✅ Удалены из документации оба блока про долг, добавленные прошлой
   итерацией:
   - `docs/en/getting-started.md` — абзац, начинающийся
     `**Known debt.** Four scenarios — ...`, целиком, вместе с пустой
     строкой после него; соседний текст (абзац про `bots_bridge.json` и
     калибровку порогов) не трогать;
   - `docs/ru/getting-started.md` — симметричный абзац
     `**Известный долг.** Четыре сценария — ...`.
3. ✅ Проверено, что в `docs/` больше нет упоминаний:
   `grep -rn "Known debt\|Известный долг\|predictionDrift" docs/` — должен
   остаться только штатный текст про контракт `predictionDrift` в описании
   сценариев, без списка «четырёх сценариев».
4. Базовая линия на момент выноса долга (`npm run sim:scenarios`,
   `@vimp-games/tanks 0.19.0`, `vimp-engine-core 0.14.1`) — эталон, с
   которым сравнивается каждый следующий этап:

```
scenarios with broken contracts: bridge.json, crosslevel.json, fall.json, terraces_climb.json

bridge.json        348 реконсиляций, 1 нарушение,
                   max |Δ| [0.4633, 0.8986, 0.2063, 12.7789, 18.2506, 2.8059, 0, 0.0208]
crosslevel.json    348 реконсиляций, 2 нарушения (sock-p1),
                   max |Δ| [1.7361, 0.8986, 0.2063, 203.2456, 18.2506, 2.8059, 0, 0.0208]
fall.json          248 реконсиляций, 1 нарушение,
                   max |Δ| [0.4633, 0.8986, 0.2063, 12.7789, 18.2506, 2.8059, 0, 0.0208]
terraces_climb     198 реконсиляций, 24 нарушения,
                   max |Δ| [2.2314, 0, 0, 27.9209, 0, 0, 0, 0.15]
```

   Порядок компонент — `[x, y, angle, vx, vy, angvel, z, level]`.
   Остальные 8 сценариев зелёные, их состояние ухудшать нельзя.

Журнал не трогать: удаление абзаца из docs — не изменение поведения.

## Этап 1. Стенд: воспроизвести дефект без прогона сценариев ✅ выполнен

Цель — красный тест на 30 строк вместо сценария на 1400 тиков, чтобы
править решатель с обратной связью в секунду, а не в минуту.

Место: `core/src/client/predictor.rs`, модуль `mod parity` (около строки
2349). Там уже есть готовый харнесс, который гоняет НАСТОЯЩИЙ хост
(`GameState = vimp_engine_core::game::EngineSim<TanksGame>`, Rapier) и
реплику (`Predictor`) на одном вводе и сравнивает состояния:
`simulate(steps, schedule)` и `expect_close(core, replica, tolerance)`.

1. Добавить `simulate_on_map(map_json, spawn, schedule, steps)`: то же, что
   `simulate`, но перед спавном зовёт `game.load_map(map_json)`
   (`EngineSim::load_map`, `packages/engine/core/src/game.rs:86`), а
   предиктору отдаёт ту же карту — как это делает `apply_map` в
   `mod tests` (`predictor.rs:1486`): распарсить `ClientMapConfig`,
   `cfg.take_levels()`, `p.set_map(&cfg, Rc::new(levels))`.
2. Фикстура карты: полоса стены длиной в 40+ тайлов по одной строке
   (склеивается в один блок, как перила `overpass`) плюс свободная полоса
   перед ней. Проще всего взять размеры реальной карты: `step: 32`,
   `scale: 0.4` (тайл 12.8 мировых единиц), стена в строке 31, колонки
   6..48 — тогда блок получится ровно `(345.6, 403.2)` ± `(268.8, 6.4)`,
   как в замере.
3. Тест `grazing_corner_hit_matches_the_host`: поставить танк в
   `x = 72.61, y = 395.07`, скорость `(148.85, 0.87)`, уровень 1, прогнать
   3—5 шагов на обеих сторонах и сравнить `expect_close` с допусками
   сценария (`angle` 0.06, `angvel` 1.5). Тест обязан быть КРАСНЫМ и
   печатать дельты — это фиксация дефекта.
4. Второй тест — под `terraces_climb`: фикстура карты `terraces` (или её
   срез), въезд на прогон `rampSteep` с теми же входными данными, что даёт
   сценарий; допуски `x` 3, `vx` 25. Если воспроизвести не удаётся,
   сузить поиск инструментацией самого сценария
   (`npm run sim -- --scenario tests/scenarios/terraces_climb.json`
   печатает `serverTime` каждой реконсиляции; смотреть первую).

Критерий этапа: оба теста красные, дельты совпадают с числами сценариев.
`#[ignore]` на них не ставить, пока не начаты правки; если этап 2 длинный —
пометить `#[ignore = "чинится в plan/prediction-drift.md, этап 2"]`, чтобы
`npm run core:test` оставался зелёным.

**Как сделано.** `simulate_on_map(map_json, pose, steps, schedule)` в
`mod parity`: карта грузится в хост через `EngineSim::load_map`, в реплику —
той же строкой через `apply_map`, стартовая поза ставится на теле Rapier и
ею же сеется реплика (`on_server_state`, offset 0 → реплей пуст). Фикстура
`long_wall_map()` — стена в строке 31, колонки 6..48 при `step 32` /
`scale 0.4`: блок ровно `(345.6, 403.2) ± (268.8, 6.4)`, как в замере.
Тест `grazing_corner_hit_matches_the_host` был красным на
`angle Δ0.1530 > 0.06` (реплика −0.0018 против 0.1513 у хоста) —
воспроизведение подтвердило, что корпус у реплики не разворачивался ВООБЩЕ.
Второго теста под `terraces_climb` не понадобилось: причина нашлась
арифметикой по записям сценария (см. этап 4).

Горизонт прогона в итоговом тесте — 4 шага: ровно столько реплика идёт
свободно между авторитетными кадрами (снапшот 30 Гц при шаге 120 Гц), и на
этом же горизонте её сравнивает контракт `predictionDrift`. Рядом —
`the_hit_frame_spins_the_hull_like_the_host` на один шаг: он держит именно
тот кадр удара, на котором `angvel` реплики была нулевой.

## Этап 2. Спекулятивные контакты в реплике (E, затем T) ✅ выполнен

Это главная правка. Реплика обязана видеть контакт ДО перекрытия, как
Rapier, и решать скорости до интеграции позиции.

### 2.1 Движок: контакты с зазором

`packages/engine/core/src/client/collision.rs`:

- `obb_vs_obb(a, b)` сейчас возвращает `None`, как только перекрытие по
  любой оси `<= 0` (строка с `if overlap <= 0.0 { return None; }`).
  Добавить вариант с предсказанием: `obb_vs_obb_within(a, b, prediction)` —
  ось с максимальным ЗАЗОРОМ выигрывает, `Contact::depth` становится
  знаковым (`> 0` — проникновение, `< 0` — зазор), контакт возвращается,
  пока зазор `<= prediction`. Старую функцию оставить обёрткой
  `obb_vs_obb_within(a, b, 0.0)` — её зовут игры и тесты.
- То же для сбора со стенами: `collect_block_contacts(obb, blocks,
  prediction)`. Сигнатура публичная и уже используется танками —
  это ломающее изменение, значит минор крейта.
- Точку контакта считать той же `contact_point` с
  `CONTACT_MANIFOLD_RATIO`: смешивание вершин грани уже сделано.

`packages/engine/core/src/client/rigid_body.rs`:

- `separate_bodies` должен игнорировать контакт с отрицательной глубиной
  (разводить нечего).
- `apply_contact_impulse` — учесть зазор: сейчас импульс не считается при
  `vn >= 0.0`. Для спекулятивного контакта порог сдвигается на скорость
  закрытия зазора за шаг: `bias = -depth / dt` при `depth < 0`, импульс
  считается от `vn - bias` и зажимается снизу нулём, чтобы контакт не
  «притягивал». Функции нужен `dt` — добавить параметр (ломающее
  изменение, тот же минор).
- Восстановление `restitution` на спекулятивном контакте применять только
  к части скорости, которая реально закрывает зазор, иначе тело отскочит,
  не коснувшись.

Тесты движка (рядом с существующими в тех же файлах):

- контакт с зазором `0.5` при `prediction = 1.0` найден, `depth < 0`,
  нормаль та же, что у перекрытия на той же паре;
- `separate_bodies` не двигает тела на отрицательной глубине;
- тело, летящее в стену со скоростью, за шаг перекрывающей зазор,
  останавливается У стены, а не внутри неё (сравнить с Rapier тем же
  приёмом, что `map::tests::soft_ccd_prediction_keeps_penetration_shallow`);
- прежнее поведение при `prediction = 0.0` бит в бит (регрессия на
  существующих тестах `collision`/`rigid_body`).

### 2.2 Танки: порядок шага

`core/src/client/predictor.rs`:

- `resolve_world` вызывается ПОСЛЕ интеграции (:777). Спекулятивный контакт
  требует обратного порядка Rapier: собрать контакты на позе НАЧАЛА шага с
  запасом `prediction` → решить импульсы → интегрировать позицию →
  один проход позиционной коррекции на остаточное проникновение.
  Развести `resolve_world` на две части (`collect_contacts` +
  `solve_and_integrate`) и переставить относительно блока интеграции
  (:768-776). Комментарий у блока интеграции про «эмпирический порядок
  Rapier, зафиксирован паритет-тестом» обязан остаться верным: позиция
  по-прежнему интегрируется скоростью ДО демпфирования.
- `prediction` брать тем же числом, что хост: `min(width, height)` корпуса
  (`core/src/tank.rs:167`), то есть `shape.half_h * 2.0`. Число обязано
  быть одно на обе стороны — вынести в общее место, а не дублировать
  литералом.
- Зеркало стражей прогона (`resolve_world`, блок `for run in &blocks`) и
  контакты с предсказанными телами (`obb_vs_obb` по парам) переводятся на
  ту же функцию с зазором — иначе стражи начнут работать иначе, чем стены.

Риск: `SOLVER_ITERATIONS = 4` (:48) подбирался под текущий порядок. Если
после перестановки появятся дрожание или залипание в углах — менять число
итераций ПОСЛЕ того, как основной дефект закрыт, и фиксировать значение
тестом, а не на глаз.

**Как сделано.** `SOLVER_ITERATIONS` трогать не понадобилось. Сверх плана в
движок добавлено накопление импульсов (`ContactImpulses`): решатель клампит
СУММУ, а не приращение итерации. Без этого первая точка манифольда забирала
весь нормальный импульс, разворачивала корпус плечом и оставляла вторую
расходящейся — реплика крутилась вдвое сильнее хоста (`w` 10.93 против
5.25), и вернуть лишнее решателю было нечем. Трение считается по скоростям
ПОСЛЕ нормального импульса и ограничивается накопленным нормальным — раньше
оно вовсе не применялось на итерациях, где нормальное ограничение уже
выполнено (ранний `return`), и реплика теряла почти всё трение контакта.

Критерий этапа: тест `grazing_corner_hit_matches_the_host` из этапа 1
зелёный; `npm run sim:scenarios` — `bridge`, `crosslevel`, `fall` зелёные;
восемь ранее зелёных сценариев остались зелёными; паритет-тесты
`mod parity` не сдвинулись.

**Итог этапа 2 в одиночку:** реплика перестала уезжать в стену (замер шага:
`vx` 148.85 → 16.1 против 6.9 у хоста), но `angle` осталась за порогом —
корпус по-прежнему не разворачивался (`w` −0.065 против 5.25). Причина
нашлась именно здесь: `contact_point` смешивает вершины ГРАНИ тела `a` и на
контакте заподлицо честно отдаёт её середину, то есть плечо ноль. Rapier же
отсекает манифольд по перекрытию граней, и обе его точки лежат ниже центра
корпуса. Отсюда этап 3 стал обязательным, а не запасным.

## Этап 3. Двухточечный манифольд ✅ выполнен

Выполнять только если после этапа 2 `angle`/`angvel` всё ещё за порогом.

Rapier на паре «куб — куб» ведёт манифольд из двух точек по клиппингу
опорных граней; реплика берёт одну точку, смешанную по
`CONTACT_MANIFOLD_RATIO`. При касательном ударе плечо получается другим,
и разойтись может именно `angvel`.

- В `client::collision` добавить `obb_manifold(a, b, prediction) ->
  ArrayVec<Contact, 2>`: найти опорную грань каждого тела по нормали,
  отсечь падающую грань плоскостями опорной (Sutherland—Hodgman по двум
  плоскостям), вернуть до двух точек с индивидуальными глубинами.
- В `predictor::resolve_world` класть в `contacts` обе точки пары; импульсы
  и так итерируются по списку.
- Тест: на разобранной позе манифольд даёт две точки, суммарный импульс
  разворачивает корпус в ту же сторону и той же величины `angvel`, что
  Rapier (сравнение через харнесс этапа 1, допуск 1.5).

**Как сделано.** `Manifold` (до двух `Contact` с общей нормалью и своими
глубинами) + `obb_manifold(a, b, prediction)`; SAT вынесен в общую
`separating_axis`, чтобы одноточечный контакт и манифольд НЕ МОГЛИ выбрать
разные оси. Референсная грань — у тела, которому принадлежит ось;
встречная отсекается двумя боковыми плоскостями, вырожденный случай
(грани не перекрываются) откатывается к прежней смешанной точке.
`collect_block_contacts` возвращает `BlockContact { manifold, block_x,
block_y }`; предиктор кладёт в импульсы КАЖДУЮ точку, а в позиционную
коррекцию — только `deepest()`, один раз на пару.

## Этап 4. `terraces_climb`: своё расхождение ✅ выполнен

Дефект отдельный: 24 нарушения из 198, `vx` Δ27.92 при пороге 25 и уровень
Δ0.15 при пороге 0.06 — то есть стороны расходятся не в одном кадре удара,
а систематически, на подъёме по крутому прогону `0 → 2`.

1. Диагностика по тесту из этапа 1 (или инструментацией сценария): в какой
   момент расходится — на входе на прогон, на смене уровня `z` через
   целое, на выезде?
2. Кандидаты, которые нужно проверить по порядку:
   - `level_state.grade(cos, sin)` в реплике против `LevelState` хоста:
     уклон под курсом считается из `RampSample::slope`, и обе стороны
     обязаны видеть одинаковый `level_height` (`MapConfig::level_height`
     едет в клиента в `set_map`);
   - момент щелчка уровня по ближайшему целому (`core/src/level.rs`,
     `state.z` → `state.level`) — если стороны щёлкают на разных шагах,
     маска столкновений и тяга расходятся на кадр;
   - тяга в горку: `motion::drive_accel(..., grade, model, &level_rules)`
     — общая формула, но входы (`throttle`, `forward_speed`) берутся из
     разных состояний.
3. Починка — там, где найдено расхождение; правило прежнее: одна формула
   на обе стороны, в общем месте (крейт или `crate::motion`), а не две
   копии.

Критерий: `terraces_climb.json` зелёный, `terraces_backside.json` и
`terraces_crate.json` остались зелёными.

**Что оказалось на самом деле.** Ни тяга, ни щелчок уровня, ни `grade`.
Все 24 записи — поле 7 (`throttle`), а `vx` было их следствием. Дельта
держалась ровно на 0.15 = 0.0833 + 0.0667: за 4 шага между кадрами реплика
СПУСКАЛА газ (`throttleDecreaseRate` 2.5 → 0.0833), пока хост его НАБИРАЛ
(`throttleIncreaseRate` 2.0 → 0.0667). Спускала потому, что
`on_server_state` объявляла танк падающим и запирала ввод: на крутом
прогоне 0 → 2 авторитетная высота дробная по определению, а вердикт
«поднимаюсь законно» брался только из откаченного состояния
(`level_state.on_ramp()`). Хост же не начинает падение на клетке прогона
вовсе — `level::step_layered` спрашивает `ramp_at` раньше любых проверок
опоры. Правка: факт «стою на прогоне» реплика тоже спрашивает у КАРТЫ под
авторитетной позицией (`levels.ramp_at(x, y)`), а не только у своего
состояния.

Сверх плана в том же месте закрыт соседний дефект: `Tank::update` в
падении выходит РАНЬШЕ башни, газа и тяги, а реплика лишь обнуляла маску
клавиш — то есть доводила центрирование башни, спускала газ и тормозила
тягой всё падение. Теперь зеркалит ранний выход
(`a_falling_replica_freezes_the_input_exactly_like_the_host`).

## Этап 5. Прогон, документация, журналы, релизы ✅ выполнен

1. ✅ **E:** `cargo test --workspace` (194 теста), `cargo clippy --workspace`
   (новых предупреждений нет — единственное, `the loop variable y`, было и
   до правок), `npx eslint .`, `npx vitest run` (2332 теста) — зелёные.
2. ✅ **Релиз E:** `vimp-engine-core 0.15.0` опубликован (минор — правки
   меняют публичные сигнатуры `client::collision` и `client::rigid_body`).
   Релиз шёл ДВУМЯ проходами: `npm run release -- --only=crate` сперва,
   потому что preflight не пускает `[patch.crates-io]` в выбранной игре, а
   снять патч до публикации крейта нельзя — танки собирались от него.
   Игры выбираются только при `--only`, включающем `games`
   (`scripts/release.js:438`), поэтому первый проход про них не спрашивает.
3. ✅ **T:** `[patch.crates-io]` снят, `core/Cargo.toml` →
   `vimp-engine-core = "0.15.0"`, `Cargo.lock` перезаписан на реестровую
   версию (`cargo fetch`; `cargo update -p` на пропатченном имени не
   работает). Прогон от опубликованного крейта: `core:test` 238 + 45,
   `eslint`, `npm test` 303, `npm run build`, `npx vimp-contract --strict`
   36/36, `npm run sim:scenarios` — 10 из 11.
4. ✅ **Документация:** T `docs/en|ru/getting-started.md` — таблица
   сценариев и раздел про `divergence: null` (почему взрыв не
   предсказывается и почему сценарии разделены); E `docs/en|ru/core.md` —
   спекулятивные контакты,
   манифольд, накопленные импульсы, почему `prediction` обязан совпадать с
   `soft_ccd_prediction`; T `docs/en|ru/core.md` — порядок шага реплики и
   зеркало раннего выхода при падении. Обе локали.
5. ✅ **Журналы:** E — `Breaking`/`Migration`/`Added`/`Fixed`; T — три
   записи в `### Fixed` неопубликованной `0.19.0` (касательный удар, газ на
   крутом прогоне, замороженный ввод в падении) и требование крейта поднято
   до `0.15.0`.
6. ✅ **Закрытие плана:** все этапы выполнены; план переезжает в
   `plan/done/`.

## Остаток `crosslevel.json` — разобран (✅ выполнен)

Единственное нарушение, пережившее этапы 2—4, оказалось НЕ дефектом
контактов, вопреки исходной гипотезе плана, — это авторитетный импульс
взрыва собственной бомбы:

- `#3 Δ-203.2456 > 25 (predicted 0.0031, authoritative 203.2487)` — поле 3
  это `vx`;
- сценарий жмёт `fire` на тике 1040 (8666.67 мс) активным `w2`
  (`src/data/weapons.js`: `type: 'explosive'`, `time: 300`,
  `impulseMagnitude: 2000000`, `radius: 50`);
- 8666.67 + 300 = **8966.67 мс** — ровно `serverTime` нарушения. Танк
  подорвал сам себя и получил импульс от хоста.

Реплика такого предсказать не может по устройству: клиент предсказывает
только ВИЗУАЛЬНЫЙ спавн бомбы (`client/shot.rs`, локальный алиас до
подтверждения кадром), а детонация, урон и импульсы авторитетны.

Случай был намеренным покрытием, а не случайностью: `p1` стоит с тика 860,
на 1000 переключает оружие, на 1040 роняет бомбу под себя, и `dumpTicks`
содержит **1075** — кадр прямо перед детонацией. Поэтому «сдвинуть
выстрел» отпало.

**Решение (выбрано пользователем): разделить сценарии.** Порогов не
трогали.

- `tests/scenarios/selfblast.json` — исходный `crosslevel.json` целиком
  (seed, таймлайн, дампы, 1400 тиков), с `divergence: null`. Опция штатная
  и документированная: контракт отчитывается ⏭️ с явной причиной, а не ❌.
  Проверено по дампам: на тике 1075 танк стоит в (79.6, 391.2) с нулевой
  скоростью, к 1400 он в (130.2, 367.1) — взрывом отбросило.
- `tests/scenarios/crosslevel.json` — тот же прогон БЕЗ тиков 1000/1040 и
  без дампа 1075; `unusedSnapshotKeys` дополнен `w2`/`w2e`. Межуровневая
  стрельба и её 696 реконсиляций остались под детектором дрейфа.

Отвергнутые варианты: механизм в движке (`divergence.expectedImpulses`) —
честнее всех, но это ещё один минор крейта и полный цикл релиза;
предсказание самоподрыва в реплике — перенос авторитетной логики в
предсказание, отдельная задача.

`npm run sim:scenarios` — **12 сценариев из 12 зелёные**, `exit 0`.

## Критерии приёмки

- [x] Долга нет в `docs/` (этап 0), он живёт в `plan/prediction-drift.md`.
- [x] `npm run sim:scenarios` — 12 из 12 зелёных, `divergence.thresholds`
      в `tests/scenarios/*.json` НЕ тронуты.
- [x] `npm run core:test` (238 + 45 тестов) и `npm test` (303 теста)
      зелёные; паритет-тесты `mod parity` не ослаблены (допуски прежние).
- [x] E: `cargo test --workspace`, `cargo clippy --workspace`, `eslint`,
      `vitest` зелёные.
- [x] `docs/en` и `docs/ru` синхронны, журналы обоих репозиториев
      оформлены.
- [x] Крейт `0.15.0` опубликован, `[patch.crates-io]` снят, танки зависят
      от него по версии.

## Что оказалось не тем, чем считал план

- **Нарушение `#3 Δ-203` в `crosslevel.json`** план относил к касательному
  удару. На деле это импульс взрыва собственной бомбы — авторитетное
  событие, а не дефект (см. раздел выше).
- **Нарушения `terraces_climb`** план относил к тяге в горку и щелчку
  уровня. На деле все 24 записи — одно следствие: реконсиляция объявляла
  падением дробную высоту крутого прогона, запирала ввод, и реплика теряла
  0.15 газа на каждом кадре (декремент 0.0833 против инкремента 0.0667 —
  ровно наблюдавшаяся дельта). Расхождение `vx` было следствием газа и
  ушло вместе с ним.
- **Одного этапа 2 не хватило.** Спекулятивный контакт остановил реплику у
  стены, но не развернул корпус: единственная точка контакта смешивалась в
  СЕРЕДИНУ грани (`CONTACT_MANIFOLD_RATIO`), плеча не было вовсе. Понадобился
  этап 3 (манифольд) и сверх плана — накопленные импульсы решателя
  (`ContactImpulses`): на приращениях первая точка манифольда забирала весь
  импульс и крутила корпус вдвое сильнее хоста.

## Если не получится

Дрейф прячет реконсиляция, и дефект стоил бы одного кадра рывка — он не
блокирующий. Если после этапов 2—3 `angle`/`angvel` останутся за порогом,
**пороги не ослаблять**: остановиться, записать в план измеренную разницу
(какой контакт, какие нормали и плечи у обеих сторон) и вернуть решение
пользователю — как это уже было сделано в прошлой итерации.

## Проверка руками (после этапа 2)

`npm run dev`, карта `overpass`: проехать по мосту вдоль перил и задеть их
углом корпуса на полном ходу. До правки в этот момент виден рывок (клиент
предсказал одно, сервер прислал другое); после — проезд вдоль перил должен
быть гладким. Второе место — `terraces`, подъём по `rampSteep` на полном
газе после этапа 4.
