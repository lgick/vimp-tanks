name: Release

# Publish via OIDC Trusted Publishing (no npm token stored). Local prep —
# bump `version` in package.json, write the changelog entry,
# `cargo update -p vimp-engine-core` / `npm i -D vimp-engine@^X` if the
# engine moved, commit, tag, push — is still done by `npm run release` in
# the engine repo (or by hand). This workflow does not create the tag — it
# runs FROM one: the tag push (`vX.Y.Z`) is what triggers it. It only builds
# the WASM core + dist/ (not committed) and publishes.
on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2

      # GITHUB_ENV, not an ${{ }} expression inside run: — a tag name is
      # attacker-shaped text, and an expression is pasted into the script
      # before bash parses it
      - name: Read version
        run: echo "VERSION=${GITHUB_REF_NAME##v}" >> "$GITHUB_ENV"

      # The tag triggers this run, but npm publishes the version in the
      # file: a tag on a commit carrying another version would publish that
      # one, or fail on a confusing "cannot publish over". Checked before any
      # install — a wrong tag should cost nothing.
      - name: Verify tag matches package version
        run: |
          file=$(jq -r .version package.json)
          if [ "$file" != "$VERSION" ]; then
            echo "tag $GITHUB_REF_NAME, but package.json says $file" >&2
            exit 1
          fi

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          registry-url: https://registry.npmjs.org

      - name: Setup Rust (vimp-tanks-core)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      # A tag run sees only its own tag's caches and main's — never those of
      # earlier release tags, so a cache saved here would never be read.
      # Instead it restores what test.yml's integration job saved on main:
      # the same `npm run core:build`, the same shared-key. rust-cache falls
      # back to the newest entry when Cargo.lock differs, which it always
      # does after a vimp-engine-core bump.
      - name: Cache cargo (saved by test.yml on main)
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: wasm-core
          save-if: false

      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

      - name: Install dependencies
        run: npm ci

      # Trusted Publishing needs npm >= 11.5.1; older npm silently falls back
      # to the legacy token flow and the publish fails with ENEEDAUTH
      - name: Update npm
        run: npm install -g npm@latest

      - name: Guard against re-publish
        run: |
          set -e
          published=$(npm view "@vimp-games/tanks@$VERSION" version 2>/dev/null || true)
          if [ -n "$published" ]; then
            echo "@vimp-games/tanks@$VERSION is already on npm" >&2
            exit 1
          fi

      - name: Build core (web + nodejs)
        run: npm run core:build

      # build:assets → copy-game-sounds.js requires build/sounds/, produced
      # by audio:process (ffmpeg loudness normalization of assets/audio-raw/)
      # — gitignored, so a fresh checkout never has it. This game predates
      # the scaffold template's placeholder-sound fallback, so there is
      # nothing to fall back to; the real pipeline has to run here.
      - name: Install ffmpeg
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends ffmpeg

      - name: Process game audio
        run: npm run audio:process

      - name: Build plugin (dist/ + manifest)
        run: npm run build

      # prepack (scripts/check-pack.js) runs automatically here and asserts
      # the tarball actually contains manifest.json, core-node/*, map assets
      - name: Publish to npm
        run: npm publish --provenance
